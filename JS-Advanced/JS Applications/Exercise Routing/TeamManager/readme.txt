1. Open the whole project with VS code.
2. Open Terminal and add sammy handlebars and bootstrap with this command --> npm install sammy handlebars bootstrap
3. Run it with live server
4. Don't change anything in the files if you want to use your base just replace it in the file located in ./scripts/url.js
