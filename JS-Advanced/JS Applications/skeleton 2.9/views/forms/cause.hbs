<div class="card mt-4">
    <img style="max-width: 1200px; height: auto; border-radius:25px;" class="card-img-top img-fluid"
        src="{{pictureUrl}}" alt="">
    <div class="card-body">
        <h3 class="card-title">{{cause}}</h3>
        <p class="card-text">{{description}}</p>
        <a href="#/details/{{_id}}" class="btn btn-success">Become a donor $$$</a>
    </div>
</div>